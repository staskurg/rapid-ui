/**
 * TypeScript types for UI Spec
 * 
 * These types are re-exported from the schema for convenience.
 * The source of truth is the Zod schema in schema.ts
 */

export type {
  FieldType,
  Field,
  TableConfig,
  FormConfig,
  UISpec,
} from "./schema";
