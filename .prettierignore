# Cursor agent context & plans (optional to format; avoids churn on tables)
.cursor

# dependencies & tooling
node_modules
.pnp.*
.yarn

# build & output
.next
out
build
dist
coverage

# generated / large local data
*.tsbuildinfo
.vercel
eval/reports
eval/fixtures/failures
tests/compiler/fixtures
tests/compiler/golden-specs
scripts/corpus-data

# long one-off doc (optional to format)
YC_AI_TRANSCRIPT.md

# lockfiles (unchanged by hand; skip slow formatting)
package-lock.json
pnpm-lock.yaml
yarn.lock
