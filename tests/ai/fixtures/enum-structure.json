{
  "entity": "Order",
  "fields": [
    {
      "name": "id",
      "label": "ID",
      "type": "number",
      "required": true
    },
    {
      "name": "status",
      "label": "Status",
      "type": "enum",
      "required": true,
      "options": ["pending", "processing", "completed", "cancelled"]
    },
    {
      "name": "total",
      "label": "Total",
      "type": "number",
      "required": true
    }
  ],
  "table": {
    "columns": ["id", "status", "total"]
  },
  "form": {
    "fields": ["status", "total"]
  },
  "filters": ["status", "total"]
}
